function mountPlayer(videoId,buttonId,url){var video=document.getElementById(videoId),button=document.getElementById(buttonId),ready=false,hlsInstance=null;if(!video)return;function load(){if(ready)return;ready=true;if(video.canPlayType("application/vnd.apple.mpegurl")){video.src=url}else if(window.Hls&&Hls.isSupported()){hlsInstance=new Hls({enableWorker:true,lowLatencyMode:true});hlsInstance.loadSource(url);hlsInstance.attachMedia(video)}else{video.src=url}}function hide(){if(button)button.classList.add("is-hidden")}function play(){load();hide();var p=video.play();if(p&&p.catch)p.catch(function(){})}if(button)button.addEventListener("click",play);video.addEventListener("click",function(){if(video.paused)play()});video.addEventListener("play",hide);window.addEventListener("pagehide",function(){if(hlsInstance)hlsInstance.destroy()})}